# Blueprint de Render: sitio estático (HTML/CSS/JS en la raíz del repo)
# Docs: https://render.com/docs/yaml-spec
services:
  - type: web
    runtime: static
    name: world-explorer
    rootDir: .
    buildCommand: "true"
    staticPublishPath: ./
